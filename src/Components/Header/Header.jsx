import './Header.css'

import React from 'react'

const Header = (props) => {
  return (
        <header className="app-header">
            <h1>Flixster</h1>
        </header>
  )
}

export default Header